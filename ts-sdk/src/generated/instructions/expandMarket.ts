/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  getU16Decoder,
  getU16Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  type TransactionSigner,
  transformEncoder,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { DROPSET_PROGRAM_ADDRESS } from "../programs";

export const EXPAND_MARKET_DISCRIMINATOR = 9;

export function getExpandMarketDiscriminatorBytes() {
  return getU8Encoder().encode(EXPAND_MARKET_DISCRIMINATOR);
}

export type ExpandMarketInstruction<
  TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountMarketAccount extends string | AccountMeta<string> = string,
  TAccountDropsetProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountMarketAccount extends string
        ? WritableAccount<TAccountMarketAccount>
        : TAccountMarketAccount,
      TAccountDropsetProgram extends string
        ? ReadonlyAccount<TAccountDropsetProgram>
        : TAccountDropsetProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ExpandMarketInstructionData = {
  discriminator: number;
  /** The number of free sectors to add to the market account. */
  numSectors: number;
};

export type ExpandMarketInstructionDataArgs = {
  /** The number of free sectors to add to the market account. */
  numSectors: number;
};

export function getExpandMarketInstructionDataEncoder(): FixedSizeEncoder<ExpandMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["numSectors", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: 9 }),
  );
}

export function getExpandMarketInstructionDataDecoder(): FixedSizeDecoder<ExpandMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["numSectors", getU16Decoder()],
  ]);
}

export function getExpandMarketInstructionDataCodec(): FixedSizeCodec<
  ExpandMarketInstructionDataArgs,
  ExpandMarketInstructionData
> {
  return combineCodec(
    getExpandMarketInstructionDataEncoder(),
    getExpandMarketInstructionDataDecoder(),
  );
}

export type ExpandMarketInput<
  TAccountEventAuthority extends string = string,
  TAccountPayer extends string = string,
  TAccountMarketAccount extends string = string,
  TAccountDropsetProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The event authority PDA signer. */
  eventAuthority: Address<TAccountEventAuthority>;
  /** The account paying for the market account expansion. */
  payer: TransactionSigner<TAccountPayer>;
  /** The market account PDA. */
  marketAccount: Address<TAccountMarketAccount>;
  /** The dropset program. */
  dropsetProgram: Address<TAccountDropsetProgram>;
  /** The system program. */
  systemProgram: Address<TAccountSystemProgram>;
  numSectors: ExpandMarketInstructionDataArgs["numSectors"];
};

export function getExpandMarketInstruction<
  TAccountEventAuthority extends string,
  TAccountPayer extends string,
  TAccountMarketAccount extends string,
  TAccountDropsetProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS,
>(
  input: ExpandMarketInput<
    TAccountEventAuthority,
    TAccountPayer,
    TAccountMarketAccount,
    TAccountDropsetProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ExpandMarketInstruction<
  TProgramAddress,
  TAccountEventAuthority,
  TAccountPayer,
  TAccountMarketAccount,
  TAccountDropsetProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    marketAccount: { value: input.marketAccount ?? null, isWritable: true },
    dropsetProgram: { value: input.dropsetProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("marketAccount", accounts.marketAccount),
      getAccountMeta("dropsetProgram", accounts.dropsetProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getExpandMarketInstructionDataEncoder().encode(
      args as ExpandMarketInstructionDataArgs,
    ),
    programAddress,
  } as ExpandMarketInstruction<
    TProgramAddress,
    TAccountEventAuthority,
    TAccountPayer,
    TAccountMarketAccount,
    TAccountDropsetProgram,
    TAccountSystemProgram
  >);
}

export type ParsedExpandMarketInstruction<
  TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The event authority PDA signer. */
    eventAuthority: TAccountMetas[0];
    /** The account paying for the market account expansion. */
    payer: TAccountMetas[1];
    /** The market account PDA. */
    marketAccount: TAccountMetas[2];
    /** The dropset program. */
    dropsetProgram: TAccountMetas[3];
    /** The system program. */
    systemProgram: TAccountMetas[4];
  };
  data: ExpandMarketInstructionData;
};

export function parseExpandMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpandMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      eventAuthority: getNextAccount(),
      payer: getNextAccount(),
      marketAccount: getNextAccount(),
      dropsetProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getExpandMarketInstructionDataDecoder().decode(instruction.data),
  };
}
