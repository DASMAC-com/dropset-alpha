/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  type TransactionSigner,
  transformEncoder,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { DROPSET_PROGRAM_ADDRESS } from "../programs";

export const FLUSH_EVENTS_DISCRIMINATOR = 8;

export function getFlushEventsDiscriminatorBytes() {
  return getU8Encoder().encode(FLUSH_EVENTS_DISCRIMINATOR);
}

export type FlushEventsInstruction<
  TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEventAuthority extends string
        ? ReadonlySignerAccount<TAccountEventAuthority> &
            AccountSignerMeta<TAccountEventAuthority>
        : TAccountEventAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type FlushEventsInstructionData = { discriminator: number };

export type FlushEventsInstructionDataArgs = {};

export function getFlushEventsInstructionDataEncoder(): FixedSizeEncoder<FlushEventsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: 8 }),
  );
}

export function getFlushEventsInstructionDataDecoder(): FixedSizeDecoder<FlushEventsInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getFlushEventsInstructionDataCodec(): FixedSizeCodec<
  FlushEventsInstructionDataArgs,
  FlushEventsInstructionData
> {
  return combineCodec(
    getFlushEventsInstructionDataEncoder(),
    getFlushEventsInstructionDataDecoder(),
  );
}

export type FlushEventsInput<TAccountEventAuthority extends string = string> = {
  /** The event authority PDA signer. */
  eventAuthority: TransactionSigner<TAccountEventAuthority>;
};

export function getFlushEventsInstruction<
  TAccountEventAuthority extends string,
  TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS,
>(
  input: FlushEventsInput<TAccountEventAuthority>,
  config?: { programAddress?: TProgramAddress },
): FlushEventsInstruction<TProgramAddress, TAccountEventAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("eventAuthority", accounts.eventAuthority)],
    data: getFlushEventsInstructionDataEncoder().encode({}),
    programAddress,
  } as FlushEventsInstruction<TProgramAddress, TAccountEventAuthority>);
}

export type ParsedFlushEventsInstruction<
  TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The event authority PDA signer. */
    eventAuthority: TAccountMetas[0];
  };
  data: FlushEventsInstructionData;
};

export function parseFlushEventsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFlushEventsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { eventAuthority: getNextAccount() },
    data: getFlushEventsInstructionDataDecoder().decode(instruction.data),
  };
}
