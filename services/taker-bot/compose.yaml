services:
  taker-bot:
    build:
      context: ../../
      dockerfile: services/Dockerfile
      args:
        SERVICE: taker-bot
    image: dropset-taker-bot
    container_name: taker-bot
    restart: unless-stopped
    network_mode: host
    volumes:
      - ../shared/config.toml:/app/services/shared/config.toml:ro
      - ./config.toml:/app/services/taker-bot/config.toml:ro
      - ./keypair.json:/app/services/taker-bot/keypair.json:ro
      - ./keypairs:/app/services/taker-bot/keypairs:ro
    healthcheck:
      test: ["CMD", "/app/service", "--health-check"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
